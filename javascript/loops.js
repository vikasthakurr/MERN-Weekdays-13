let arr = [1, 2, 3, 4, 4, 5, 5];

// Loops are used to execute a block of code repeatedly until a condition is met.
// `for` loop syntax: `for (initialization; condition; update)`.
// Initialization runs once, condition is checked before every iteration, and update runs after each iteration.
// for(initial; condition; i++)

// for (let i = 0; i < arr.length; i++) {
//   console.log(arr[i]);
// }

// `forEach` is an array method that runs a callback function once for each element.
// It is best for iteration when you do not need `break` or `continue`.
// arr.forEach((ele)=>{
//     console.log(ele)
// })

// let obj = {
//   fname: "vikas",
//   lname: "thakur",
// };

// `for...in` loop iterates over keys (property names) of an object.
// for (let key in obj) {
//   console.log(obj[key]);
// }
