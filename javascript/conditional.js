let age = 180;
// Conditional statements run different blocks of code based on whether a condition is true or false.
// `if` checks the first condition, `else if` checks additional conditions, and `else` runs when none match.
// if (age > 18) {
//   console.log("eligible to vote");
// } else if (age == 18) {
//   console.log("you are a teenager but can vote");
// } else {
//   console.log("not eligible to vote");
// }

// Ternary operator (`condition ? valueIfTrue : valueIfFalse`) is a short form of simple if-else.
// console.log(age >= 18 ? "can vote" : "cant vote");

// `switch` is used when one value is compared against multiple fixed cases.
// `break` stops execution so the next case does not run automatically.
// switch (age) {
//   case 10:
//     console.log("your are age is 10");
//     break;
//   case 18:
//     console.log("age is 18");
//     break;
//   default:
//     console.log("invalid age");
//     break;
// }

// let username = "vikas";
// let pass = 1234;

// Logical OR (`||`) returns true if at least one condition is true.
// if(username =="vikas" || pass==1234){}

// let a = 1;
// let b = "1";
// `==` checks value after type conversion, while `===` checks both value and data type (strict equality).
// console.log(a===b)
